# 개인 작업 규칙 (Rules)

이 파일은 **스킬이 아니라 항상 적용되는 행동 규칙**을 모아둡니다. 스킬은 특정
작업 유형(예: xlsx 편집, 한글 인코딩 이슈)에 매칭될 때만 발동하는 절차이고,
여기 있는 규칙은 어떤 작업을 하든 매번 적용되어야 하는 기본값입니다. 다른
프로젝트에도 같은 규칙을 적용하고 싶다면 그 프로젝트의 `CLAUDE.md`에 아래
내용을 그대로 복사해 넣으세요.

## 주요 diff가 생기면 PR 생성 여부를 먼저 확인한다

Claude Code는 기본적으로 사용자가 명시적으로 요청하지 않으면 PR을 만들지
않는다. 이 기본값 자체는 유지하되, 다음 조건을 만족하면 작업을 마친 시점에
"PR을 만들까요?"라고 먼저 물어본다:

- 여러 파일에 걸친 기능 추가/버그 수정처럼 리뷰할 가치가 있는 규모의 diff가
  만들어졌고,
- 해당 작업에 대해 PR을 만들지 말라는 명시적 지시가 없었던 경우.

사용자가 동의한 뒤에만 PR을 생성한다 — diff가 커 보인다는 이유만으로 확인 없이
바로 PR을 만들지는 않는다. PR 생성은 다른 사람에게도 보이고 되돌리기 번거로운
행동이라, "필요해 보이면 자동 생성" 대신 "필요해 보이면 먼저 제안"을 기본값으로
삼는다.

사소한 변경(오타 수정, 한 줄짜리 수정, 설정값 조정 등)에는 이 규칙을 적용하지
않는다 — 매번 물어보면 오히려 번거롭다.
